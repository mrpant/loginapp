 <section id="main-content">
      <section class="wrapper">
        <div class="row">
          <div class="col-lg-10">

            <h3 class="page-header"><i class="fa fa-files-o"></i>PRODUCT ASSIGN TO COMPANY</h3>
           
          </div>
          <div class="col-lg-2">
               <a class="btn btn-primary page-header" href="/company"> VIEW PRODUCTS</a>
          </div>
        </div>
        <!-- Form validations -->
        <div class="row">
          <div class="col-lg-12">
            <section class="panel">
              <header class="panel-heading">
             
              Assign Product to Company
              </header>
              <div class="panel-body">
                <div class="form">
                  <form class="form-validate form-horizontal" method="post" action="/company/product-assign-update/{{companyId}}">
                    {{#each companyList}}
                    <div class="form-group ">
                    <div class=" col-lg-10 col-lg-offset-2">
                        <label>
                        <input type="checkbox" value="{{_id}}" 
                            
                            {{#if isChecked}}
                                checked
                            {{/if}}
                         name="productId[]">
                           {{name}}
                           
                        </label>
                      </div>
                    </div>
                    {{/each}}
                    <div class="form-group">
                      <div class="col-lg-offset-2 col-lg-10">
                        <button class="btn btn-primary" type="submit">Save</button>
                        <button class="btn btn-default" type="button">Cancel</button>
                      </div>
                    </div>
                  </form>
                </div>

              </div>
            </section>
          </div>
        </div>
        
        <!-- page end-->
      </section>
    </section>